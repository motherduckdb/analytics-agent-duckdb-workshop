import { asset } from "@/lib/asset";

## What is Semantic Context?

The **semantic layer** — a single source of truth for metrics and definitions — is an appealing idea. But people, like water, follow the path of least resistance.

"Semantic" is just a fancy word meaning there's a **specific meaning** for certain concepts and metrics in a company. Some are easy and stable; others live in people's heads and shift over time.

The good news: **LLMs are very good at pulling data from many sources.** We can use the full *semantic context* — not just a separate layer.

---

## Where Semantic Context Already Lives

In practice, semantic context is already embedded across your tools:

<Mermaid
  caption="Semantic context sources → data warehouse"
  chart={`flowchart TB
    SC(["Semantic Context"]):::ctx
    DBT["dbt<br/><span style='font-size:11px;color:#A1A1A1'>models &amp; tests</span>"]:::src
    BI["BI<br/><span style='font-size:11px;color:#A1A1A1'>charts &amp; metrics</span>"]:::src
    AN["Analyst<br/><span style='font-size:11px;color:#A1A1A1'>ad-hoc queries</span>"]:::src
    DW[("Your Data Warehouse<br/><span style='font-size:11px;color:#A1A1A1'>DuckDB · Snowflake · BigQuery</span>")]:::dw

    SC --> DBT
    SC --> BI
    SC --> AN
    DBT --> DW
    BI --> DW
    AN --> DW

    classDef ctx fill:#FFDE00,stroke:#383838,stroke-width:2px,color:#383838,font-weight:700
    classDef src fill:#FFFFFF,stroke:#6FC2FF,stroke-width:1.5px,color:#383838
    classDef dw fill:#383838,stroke:#16AA98,stroke-width:2px,color:#FFFFFF,font-weight:700
  `}
/>

You don't need a complex data application to create this. It can start as simple as **comments on columns and tables** in your data warehouse that you let your LLM generate monthly from your sources.

---

## Two Layers of Context

The model can write SQL. The loop works. What broke it was **the data**. Two layers of context fix this:

| Layer | What it does | Where it lives |
|-------|-------------|----------------|
| **Skill file** | Which tables to use, business rules | Markdown &rarr; system prompt |
| **COMMENT ON** | What each column means | In the `.duckdb` file |

---

## The Loop, Upgraded

Three changes turn the bare loop into one that accepts context and works with every provider:

<CopyBlock label="agent_loop.py — with skill file" highlight={[3,20,33,34]} text={`from dotenv import load_dotenv; load_dotenv()
from openai import OpenAI  # OpenAI SDK → any OpenAI-compatible endpoint
import duckdb, json, os, sys

# Default: OpenRouter + GPT-5.4-nano
client = OpenAI(
    base_url="https://openrouter.ai/api/v1",
    api_key=os.environ["OPENROUTER_API_KEY"],
)
MODEL = os.environ.get("OPENROUTER_MODEL", "openai/gpt-5.4-nano")

con = duckdb.connect("duckoffee.duckdb", read_only=True)

tools = [{"type": "function", "function": {
    "name": "query_duckdb",
    "description": "Execute a read-only SQL query on the Duckoffee database.",
    "parameters": {"type": "object", "properties": {"sql": {"type": "string"}}, "required": ["sql"]},
}}]

skill = open("skill.md").read() if os.path.exists("skill.md") else ""

def run_tool(name, args):
    if name == "query_duckdb":
        try:
            rows = con.execute(args["sql"]).fetchall()
            cols = [d[0] for d in con.description]
            return json.dumps({"columns": cols, "rows": rows[:50]}, default=str)
        except Exception as e:
            return json.dumps({"error": str(e)})

def ask(question):
    messages = []
    if skill:
        messages.append({"role": "system", "content": skill})
    messages.append({"role": "user", "content": question})
    while True:
        response = client.chat.completions.create(model=MODEL, tools=tools, messages=messages)
        choice = response.choices[0]
        if choice.message.content is None:
            choice.message.content = ""
        messages.append(choice.message)
        if choice.finish_reason != "tool_calls":
            return choice.message.content
        for call in choice.message.tool_calls:
            args = json.loads(call.function.arguments)
            print(f"  tool: {call.function.name}: {args.get('sql', '')[:70]}")
            result = run_tool(call.function.name, args)
            messages.append({"role": "tool", "tool_call_id": call.id, "content": result})

question = sys.argv[1] if len(sys.argv) > 1 else "What are the top 5 best-selling products?"
print(ask(question))`} />

| Change | Why |
|--------|-----|
| `import sys` + `sys.argv[1]` | Accept question from CLI |
| `skill = open("skill.md")...` &rarr; system prompt | Inject business rules before the question |
| `if content is None: content = ""` | Ollama rejects `null` content on assistant messages |

---

## Skill File — The Guide

A skill file tells the agent: use `orders` not `raw_orders`, amounts are in euros, customer_type is `'new'` or `'returning'` (not `'repeat'`).

<CopyBlock label="Read the skill file" text="cat skill.md" />

Key rules:
- **Always prefer marts** (orders, customers, order_items) over raw/staging tables
- **Amounts in marts are euros**, raw tables are cents
- **Dead-end tables**: daily_revenue (5/20 locations, stale), customer_segments (6k/12k), order_facts (ambiguous `total`)
- **Legacy tables**: raw_legacy_invoices has SAP-style columns (AMTTTL, STSCODE, CSTCODE) — use stg_legacy_invoices instead

---

## Use the Skill

<CopyBlock label="Prompt with skill" text={`Read the file skill.md first — it has instructions for the Duckoffee database.
Then query duckoffee.duckdb.

Answer: What's the average order value?`} />

Now the agent should return **169.90 euros** (from `orders`) not ~17,000 (from `raw_orders`).

<Exercise title="Test the skill file" minutes={5}>
  Re-ask the traps from Part 2 with the skill file. Verify: "Which venue has the highest revenue?" should return **Quack Quarter (1,109,769.82 EUR)**, not "store_id 5 with 110M". "How many repeat customers?" should now correctly use `'returning'` (7,759).
</Exercise>

---

## COMMENT ON — Meaning in the Database

SQL `COMMENT ON` stores descriptions **in the database itself**. Any tool that reads metadata discovers them.

<CopyBlock label="Add comments to key tables" text={`duckdb duckoffee.duckdb << 'SQL'
-- Marts (source of truth)
COMMENT ON TABLE orders IS 'Clean orders mart. Amounts in EUROS. Pre-computed: is_food_order, count_food_items, includes_ticket. Use this, not raw_orders.';
COMMENT ON TABLE customers IS 'Customer mart. Pre-computed: lifetime_spend, count_lifetime_orders, first/last_order_at. customer_type is new or returning only.';
COMMENT ON TABLE order_items IS 'Line items with product_name, product_category, product_price, supply_cost. Join to orders on order_id.';

-- Raw (traps)
COMMENT ON TABLE raw_orders IS 'Raw orders. Amounts in CENTS (divide by 100). Column "customer" = customer_id, "store_id" = location_id.';
COMMENT ON TABLE raw_items IS 'Raw line items. Uses "sku" not "product_id".';
COMMENT ON TABLE raw_supplies IS 'Raw supplies. Uses "perishable" not "is_perishable_supply".';
COMMENT ON TABLE raw_legacy_invoices IS 'SAP-style columns: AMTTTL (cents), STSCODE (C/X/P = completed/cancelled/pending), CSTCODE, LOCCODE, DISCPCT. Use stg_legacy_invoices instead.';

-- Dead-ends
COMMENT ON TABLE daily_revenue IS 'STALE. Only 5 of 20 locations (Pond Stage, Feather & Bean, Quack Quarter, Mallard Manor, Duck Yard). No data after 2025-12-31. Use orders instead.';
COMMENT ON TABLE customer_segments IS 'INCOMPLETE. Only 6k of 12k customers. Labels (vip/regular/casual) are unreliable. Derive from customers mart.';
COMMENT ON TABLE order_facts IS 'AMBIGUOUS. Column "total" is actually subtotal, not order_total. Missing recent orders. Use orders instead.';

-- Key columns
COMMENT ON COLUMN orders.order_total IS 'Total in EUROS (subtotal + tax_paid). Use this for revenue. Identity: order_total = subtotal + tax_paid.';
COMMENT ON COLUMN orders.is_food_order IS 'Pre-computed: TRUE if all items are Food category. 52,803 food-only orders.';
COMMENT ON COLUMN orders.includes_ticket IS 'Pre-computed: TRUE if any item is a Ticket. 33.69% of orders.';
COMMENT ON COLUMN orders.count_food_items IS 'Pre-computed count of food items in this order.';
COMMENT ON COLUMN customers.customer_type IS 'Only two values: new, returning. No repeat/existing/VIP.';
COMMENT ON COLUMN customers.lifetime_spend IS 'Pre-computed total spend in EUROS. No need to SUM from orders.';
COMMENT ON COLUMN raw_orders.order_total IS 'WARNING: stored in CENTS not euros. Divide by 100.';
COMMENT ON COLUMN raw_orders.customer IS 'This is customer_id. Column lacks the _id suffix.';
COMMENT ON COLUMN raw_orders.store_id IS 'This is location_id. Name differs from orders table.';
SQL`} />

---

## Why COMMENT ON?

- **Zero infrastructure** — just SQL, stored in the `.duckdb` file
- **Portable** — share the file, share the context
- **Discoverable** — any tool reading `duckdb_columns()` gets them
- **Prevents traps** — mark dead-end tables with warnings

---

## Test It

Verify comments are stored:

<CopyBlock label="Check comments" text={`duckdb duckoffee.duckdb -c "
SELECT table_name, left(comment, 80) AS comment
FROM duckdb_tables()
WHERE comment IS NOT NULL
ORDER BY table_name;
"`} />

Now re-ask the agent with both layers:

<CopyBlock label="Prompt with skill + comments" text={`Read skill.md for business rules.
Also check database comments:
  duckdb duckoffee.duckdb -c "SELECT table_name, comment FROM duckdb_tables() WHERE comment IS NOT NULL"

Now answer: What's the daily revenue trend for the last 3 months?`} />

<Tip>Without context, the agent would use daily_revenue (stale, 5 venues). With context, it computes from orders (all 20 venues, up to date). Daily revenue should be ~21,000-30,000 euros per day in early 2026.</Tip>

<Exercise title="Test dead-end traps" minutes={5}>
  Try these with both skill + COMMENT ON: "Compare revenue across all 20 venues" (T02) — should show all 20, not just 5. "What's the breakdown of customer segments?" (T04) — should derive from customers mart, not the incomplete customer_segments table.
</Exercise>

---

## Where This Falls Short

Ask the same revenue question twice. Compare the SQL. It's different each time.

For a **board deck** or **financial report**, you need the exact same SQL every time. That's what **tools** give you — code that always produces the same result.

<Quiz
  question="Why is daily_revenue a 'dead-end' table?"
  options={[
    "It has wrong data types",
    "It only covers 5 of 20 locations and stops at 2025-12-31",
    "It's empty",
  ]}
  correct={1}
  explanation="daily_revenue is stale: it only has data for 5 of 20 venues (The Pond Stage, Feather & Bean, Quack Quarter, Mallard Manor, and The Duck Yard), and has no rows after 2025-12-31. Always compute revenue from the orders table instead."
/>

---

## What about MotherDuck?

<div style={{ display: "flex", alignItems: "center", gap: 16, marginBottom: 10 }}>
  <img src={asset("/motherduck-logo.svg")} alt="MotherDuck" style={{ height: 40, width: "auto" }} />
</div>

**DuckDB, supercharged.** MotherDuck is the serverless analytics platform built on DuckDB — run the same SQL from your laptop to a cloud data warehouse to customer-facing applications, with zero infrastructure to manage.

<CopyBlock label="One line to connect" text={`ATTACH 'md:';`} lang="sql" />

That single statement connects your local DuckDB session to MotherDuck's cloud. Query local files and cloud tables in the same SQL — the engine splits work between your machine and the cloud automatically.

<Tip>Everything you built today with a local `.duckdb` file works the same way on MotherDuck — skill files, COMMENT ON, PydanticAI tools, MCP servers. Just swap the connection string.</Tip>
