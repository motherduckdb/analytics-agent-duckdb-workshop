## Skill for Flexibility. Tools for Determinism.

The skill handles 80% of questions. For the other 20% — auditable numbers — you need **code that always produces the same result**.

The LLM decides **when** to call a tool. The code decides **what** happens.

| Feature | Raw loop | PydanticAI |
|---------|----------|------------|
| Agentic loop | Manual `while True` | Handled for you |
| Tool definition | `if name == "tool_a": ...` | `@agent.tool_plain` |
| Provider swap | Different SDK | One string change |
| Retries | Manual | Built-in |

---

## Agent with PydanticAI

<CopyBlock label="Create agent_tools.py" text={`import os, json, duckdb
from pathlib import Path
from pydantic_ai import Agent
from pydantic_ai.models.openai import OpenAIChatModel
from pydantic_ai.providers.openrouter import OpenRouterProvider

con = duckdb.connect("duckoffee.duckdb", read_only=True)

# Default: OpenRouter + Claude Sonnet
model = OpenAIChatModel(
    os.environ.get("OPENROUTER_MODEL", "anthropic/claude-sonnet-4"),
    provider=OpenRouterProvider(api_key=os.environ["OPENROUTER_API_KEY"]),
)

# BYOK alternatives — pick one:
#   model = "anthropic:claude-sonnet-4-20250514"      # direct Anthropic key
#   model = "openai:gpt-4o"                            # direct OpenAI key
#   model = "ollama:gemma4:latest"                     # fully local side-note

agent = Agent(
    model,
    system_prompt=Path("skill.md").read_text(),
)

@agent.tool_plain
def query_duckdb(sql: str) -> str:
    """Execute a read-only SQL query on the Duckoffee database."""
    try:
        result = con.execute(sql).fetchall()
        cols = [desc[0] for desc in con.description]
        return json.dumps({"columns": cols, "rows": result[:50]}, default=str)
    except Exception as e:
        return json.dumps({"error": str(e)})`} />

---

## Add: Revenue Tool

<CopyBlock label="Add to agent_tools.py" text={`@agent.tool_plain
def revenue_by_venue(year: int | None = None, city: str | None = None) -> str:
    """Revenue by venue with exact business rules. Always uses orders mart (euros)."""
    params = []
    where = []
    if year:
        where.append("extract(year FROM o.ordered_at) = ?")
        params.append(year)
    if city:
        where.append("l.city = ?")
        params.append(city)

    where_sql = ("WHERE " + " AND ".join(where)) if where else ""

    rows = con.execute(f"""
        SELECT l.location_name, l.city,
               count(*) AS orders,
               round(sum(o.order_total), 2) AS revenue
        FROM orders o
        JOIN locations l ON o.location_id = l.location_id
        {where_sql}
        GROUP BY ALL ORDER BY revenue DESC
    """, params).fetchall()

    lines = ["Revenue by venue" + (f" ({year})" if year else "") + (f" in {city}" if city else "") + ":"]
    total = 0
    for name, city_val, count, rev in rows:
        lines.append(f"  {name:<20} {city_val:<12} {count:>6} orders  {rev:>12,.2f} EUR")
        total += rev
    lines.append(f"  {'Total':<20} {'':<12} {sum(r[2] for r in rows):>6} orders  {total:>12,.2f} EUR")
    return "\\n".join(lines)`} />

<Tip>This tool always returns Quack Quarter as top venue (1,109,769.82 EUR). No more "store_id 5 with 110M" from raw_orders.</Tip>

---

## Add: Top Customers Tool

<CopyBlock label="Add to agent_tools.py" text={`@agent.tool_plain
def top_customers(n: int = 10, customer_type: str | None = None) -> str:
    """Top N customers by lifetime spend. Uses pre-computed customers mart."""
    params = []
    where = ""
    if customer_type:
        where = "WHERE customer_type = ?"
        params.append(customer_type)
    params.append(n)

    rows = con.execute(f"""
        SELECT full_name, customer_type, lifetime_spend, count_lifetime_orders
        FROM customers {where}
        ORDER BY lifetime_spend DESC LIMIT ?
    """, params).fetchall()

    lines = [f"Top {n} customers" + (f" ({customer_type})" if customer_type else "") + ":"]
    for name, ctype, spend, orders in rows:
        lines.append(f"  {name:<30} {ctype:<10} {spend:>10,.2f} EUR  ({orders} orders)")
    return "\\n".join(lines)`} />

<Tip>Uses `customers.lifetime_spend` directly — no need to SUM from orders. Top customer: Mrs. Kristal Aufderhar (8,336.24 EUR).</Tip>

---

## Add: Product Performance Tool

<CopyBlock label="Add to agent_tools.py" text={`@agent.tool_plain
def product_performance(category: str | None = None) -> str:
    """Product performance with margin. Uses order_items mart."""
    params = []
    where = ""
    if category:
        where = "WHERE product_category = ?"
        params.append(category)

    rows = con.execute(f"""
        SELECT product_name, product_category,
               count(*) AS times_sold,
               round(avg(product_price), 2) AS avg_price,
               round(avg(supply_cost), 2) AS avg_cost,
               round(avg(product_price - supply_cost), 2) AS avg_margin
        FROM order_items {where}
        GROUP BY ALL ORDER BY times_sold DESC
    """, params).fetchall()

    lines = [f"Product performance" + (f" ({category})" if category else "") + ":"]
    lines.append(f"  {'Product':<30} {'Cat':<10} {'Sold':>6} {'Price':>8} {'Cost':>8} {'Margin':>8}")
    for name, cat, sold, price, cost, margin in rows:
        lines.append(f"  {name:<30} {cat:<10} {sold:>6} {price:>8.2f} {cost:>8.2f} {margin:>8.2f}")
    return "\\n".join(lines)`} />

<Tip>Beverages and food are **loss leaders** (negative margin). Merch and tickets are profitable. Top seller: Flat Lake White (32,428 sold).</Tip>

---

## Test Your Tools

<CopyBlock label="Install deps" text="uv add pydantic-ai duckdb" />

<CopyBlock label="Test directly (no model needed)" text="uv run python agent_tools.py" />

<CopyBlock label="Test via agent" text={`uv run python agent_tools.py "Revenue by venue for 2025"
uv run python agent_tools.py "Top 5 returning customers"
uv run python agent_tools.py "What's the profit margin on beverages?"`} />

<Exercise title="Build your own tool" minutes={7}>
  Write a `food_order_stats()` tool that returns how many food-only orders there are and what percentage of all orders they represent. Use `orders.is_food_order`. Expected: **52,803 food-only orders (44.0%)**. Don't re-join to order_items — that massively undercounts (P03 trap).
</Exercise>

---

## Swap Providers

<CopyBlock label="One change, another provider" text={`# Direct Anthropic key (BYOK)
agent = Agent("anthropic:claude-sonnet-4-20250514", system_prompt=Path("skill.md").read_text())

# Local Ollama side-note
# agent = Agent("ollama:gemma4:latest", system_prompt=Path("skill.md").read_text())`} />

Same tools, same skill, different model. Your tools are portable across providers.

<Quiz
  question="Why use revenue_by_venue() instead of letting the LLM write SQL?"
  options={[
    "The LLM can't write SQL",
    "The tool always returns consistent, auditable results — same input, same output",
    "It's faster",
  ]}
  correct={1}
  explanation="Deterministic tools guarantee the same SQL runs every time. For financial reports and dashboards, consistency matters more than flexibility."
/>
