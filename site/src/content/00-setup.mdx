## What You'll Need

Pick whichever fits your setup. **Default path: OpenRouter + opencode** — works identically in the devcontainer and on your own machine.

| | **Recommended: BYOK** | **Offline side-note: Local Ollama** |
|---|---|---|
| **LLM** | Claude **Sonnet** via **OpenRouter** (or direct Anthropic key) | Gemma via Ollama (runs locally, free) |
| **Agentic client** | `opencode` (pre-installed) — also works with Claude Code | `opencode` pointed at Ollama |
| **Python** | 3.10+ (devcontainer: 3.12 pre-installed) | same |
| **Package manager** | `uv` (pre-installed in devcontainer) | same |
| **DuckDB** | Pre-installed / `uv tool install duckdb-cli` | same |
| **Where** | Codespace or local Docker or your machine | same |

<Tip>**OpenRouter = one key, every frontier model.** We use it as the default so everyone — Codespace or local — has the same flow. Bring your own Anthropic key instead if you prefer; it's a one-line swap.</Tip>

---

## Get an OpenRouter Key

1. Head to [openrouter.ai](https://openrouter.ai) → **Sign in** → **Keys** → **Create Key**.
2. Top up a few dollars of credit (Sonnet is a few cents per question).
3. Copy the `sk-or-v1-...` key.

<CopyBlock label="Export it" text={`export OPENROUTER_API_KEY="sk-or-v1-..."
# optional: pin the model once, used everywhere
export OPENROUTER_MODEL="anthropic/claude-sonnet-4"`} />

<Tip>Prefer direct Anthropic? `export ANTHROPIC_API_KEY="sk-ant-..."` — every code snippet in this workshop shows both variants.</Tip>

---

## Option A: Devcontainer (fastest)

<CopyBlock label="Open in Codespace" text={`# Click "Code" → "Codespaces" → "Create codespace on main"
# Or from CLI:
gh codespace create --repo motherduckdb/analytics-agent-duckdb-workshop`} />

Once the container is ready, export your key (see above) and verify:

<CopyBlock label="Verify" text={`uv --version
duckdb --version
opencode --version
echo "OPENROUTER_API_KEY=\${OPENROUTER_API_KEY:+set}"`} />

---

## Option B: Local Setup

<CopyBlock label="Install prerequisites" text={`# Install uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Install DuckDB CLI
uv tool install duckdb-cli

# Install opencode (agentic client that speaks OpenRouter)
curl -fsSL https://opencode.ai/install | bash

# Clone the repo
git clone https://github.com/motherduckdb/analytics-agent-duckdb-workshop.git
cd analytics-agent-duckdb-workshop/workshop`} />

<CopyBlock label="Set your key" text={`export OPENROUTER_API_KEY="sk-or-v1-..."
# or, if you prefer direct Anthropic:
# export ANTHROPIC_API_KEY="sk-ant-..."`} />

---

## Side-note: Fully Local with Ollama

If you want to demo the flow **without any API cost or network**, Ollama + Gemma is a fine fallback. It's slower and less accurate, but the code is identical — only the provider string changes.

<CopyBlock label="Pull Gemma and serve" text={`ollama serve &
ollama pull gemma4:latest`} />

Every Python snippet in this workshop includes the Ollama variant as a commented alternative.

---

## The Duckoffee Dataset

You'll work with **Duckoffee** — a chain of coffee venues at tech conferences across 13 cities worldwide.

<CopyBlock label="Quick check" text={`duckdb duckoffee.duckdb -c "SELECT location_name, city FROM locations;"`} />

The database has **multiple layers** — and some of them are traps:

| Layer | Tables | Watch out |
|-------|--------|-----------|
| **Raw** | raw_orders, raw_items, raw_supplies, raw_legacy_invoices | Amounts in **cents**, cryptic column names |
| **Staging** | stg_orders, stg_items, stg_supplies, stg_legacy_invoices | Clean names, euros |
| **Marts** | orders, customers, order_items | Pre-computed fields, the source of truth |
| **Dead-ends** | daily_revenue, customer_segments, order_facts | Stale, incomplete, or ambiguous |

Scale: **12k customers, 120k orders, 500k line items, 25 products, 20 venues, 12k legacy invoices.**

---

## The Setup

You run analytics for Duckoffee. Every week the team asks:
- *"What's our revenue by venue?"*
- *"Who are our top customers?"*
- *"Which products are profitable?"*

You spend **a day a week** on ad-hoc queries. Today we'll build an **answers machine**.

---

## What We'll Build

| Part | What | Time |
|------|------|------|
| 1 | DuckDB from Python | 10 min |
| 2 | Text-to-SQL & the agentic loop | 15 min |
| 3 | Semantic context: skill file + COMMENT ON | 15 min |
| 4 | Deterministic tools with PydanticAI | 20 min |
| 5 | Distribute via FastMCP | 15 min |

<Quiz
  question="What's special about this dataset?"
  options={[
    "It's a single clean table",
    "It has multiple layers with traps: raw (cents), staging, marts, and dead-end tables",
    "It's a CSV file",
  ]}
  correct={1}
  explanation="The Duckoffee database has raw tables (amounts in cents!), staging tables, clean marts, and dead-end tables that will trick an LLM. That's what makes semantic context valuable."
/>
